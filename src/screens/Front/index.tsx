import { StyleSheet, Text, View, TouchableOpacity, Touchable } from 'react-native';

export default function Front(){
    return(
        <View>
            <Text style={styles.text}>tetseteste</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    text: {
        fontSize: 23,
    },
})